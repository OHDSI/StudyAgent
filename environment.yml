name: study-agent
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - anyio
      - faiss-cpu
      - flask
      - mcp
      - numpy
      - omop_alchemy
      - pydantic
      - pyyaml      
      - pytest
      - sqlalchemy
      - ruff
      - requests

