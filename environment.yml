name: study-agent
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - anyio
      - faiss-cpu
      - flask
      - mcp
      - numpy
      - omop-alchemy
      - pydantic
      - pyyaml      
      - pytest
      - requests
      - ruff      
      - sqlalchemy
