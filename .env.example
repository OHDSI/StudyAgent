# Legacy secret-only compatibility template.
# Prefer `study-agent-setup`, which writes config.yaml and secrets.env.
# Do not put hosts, paths, ports, model names, or other non-secret settings here.

# LLM_API_KEY=
# EMBED_API_KEY=
# OMOP_DB_ENGINE=
# STUDY_AGENT_MCP_TOKEN=
# PV_COPILOT_TOKEN=
