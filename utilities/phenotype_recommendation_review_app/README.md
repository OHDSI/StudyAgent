# Phenotype recommendation review and testing app

Start the ACP and MCP with a phenotype index properly configured and then:

```
cd phenotype_recommendation_review_app/
python server.py
```

You can then open a browser and go to http://localhost:<port indicated in the output from startup>

Search for phenotype recommendations. 