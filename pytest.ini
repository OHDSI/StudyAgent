[pytest]
markers =
    core: Core tool unit tests
    acp: ACP layer tests
    mcp: MCP server/tool registration tests
    r_shell: Static tests for the in-repository R shells and R client packages
    r_integration: Tests requiring Rscript and optional R packages
addopts = -ra
