[pytest]
markers =
    core: Core tool unit tests
    acp: ACP layer tests
    mcp: MCP server/tool registration tests
    r_client_compatibility: Opt-in installed companion R-client compatibility checks
addopts = -ra
