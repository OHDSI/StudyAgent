Return one JSON object matching the schema.

Represent index events, OMOP domains, concept sets, temporal logic, observation requirements, attrition, exit, and era settings explicitly. Every decision must be traceable to confirmed scope or marked as an assumption. Put all conditions that qualify an index event on the entry event, not in attrition. Refuse definitions requiring cohort set operations, cross-event arithmetic, unsupported ordinal logic, or unreviewable deep nesting.

The deterministic Capr emitter supports only APIs documented in CAPR_REFERENCE.md. Do not return R code or Circe JSON in this response.
